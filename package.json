{
  "name": "glorious-gladius-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd admin && npm install && npm run build && mkdir -p ../public && cp -r dist/* ../public",
    "install": "cd admin && npm install"
  }
}
